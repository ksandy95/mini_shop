<%= link_to 'New Item', '/merchants/:merchant_id/items/new' %>

<h1> All Items </h1>
  <% @items.each do |item| %>
    <div id="Item-<%=item.id %>">
      <h2> <%= link_to "#{item.name}", "/merchants/#{merchant.id}/items" %></h2>
    </div>
  <% end %>
